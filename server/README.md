# ResultMA Backend API

Бэкенд для системы управления образованием с поддержкой OMR сканирования, AI парсинга тестов и автоматизации.

## 🚀 Возможности

- **Управление пользователями**: Роли (Super Admin, Branch Admin, Teacher, Student)
- **Управление тестами**: Создание, редактирование, импорт из Word/Excel/изображений
- **OMR сканирование**: Автоматическое распознавание ответных листов
- **AI парсинг**: Groq AI для извлечения вопросов из документов
- **Блок-тесты**: Комплексные тесты по нескольким предметам
- **Статистика**: Детальная аналитика по студентам, группам, филиалам
- **Автоматизация**: Автоматическое повышение класса студентов

## 📋 Требования

- Node.js >= 18.x
- MongoDB >= 6.x
- Python 3.x (для OMR сканирования)
- Redis (опционально, для кэширования)

## 🛠️ Установка

### 1. Установка зависимостей Node.js

```bash
cd server
npm install
```

### 2. Установка Python зависимостей

```bash
pip install -r python/requirements.txt
```

Или с использованием виртуального окружения:

```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r python/requirements.txt
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и настройте:

```bash
cp .env.example .env
```

Обязательные переменные:
- `MONGODB_URI` - строка подключения к MongoDB
- `JWT_SECRET` - секретный ключ для JWT токенов
- `PORT` - порт сервера (по умолчанию 9999)

Опциональные переменные:
- `GROQ_API_KEY` - API ключи Groq для AI парсинга (можно несколько через запятую)
- `REDIS_ENABLED` - включить Redis кэширование (true/false)
- `REDIS_HOST` - хост Redis сервера
- `REDIS_PORT` - порт Redis сервера

### 4. Инициализация базы данных

```bash
# Создать индексы
npm run create-indexes

# Инициализировать роли
npm run init-roles

# Создать администратора
npm run create-admin
```

## 🏃 Запуск

### Development режим

```bash
npm run dev
```

Сервер запустится на `http://localhost:9999` с hot-reload.

### Production режим

```bash
# Сборка
npm run build

# Запуск
npm start
```

## 📚 API Endpoints

### Health Check

- `GET /api/health` - Базовая проверка здоровья
- `GET /api/health/detailed` - Детальная информация о всех сервисах
- `GET /api/health/ready` - Readiness probe (для Kubernetes)
- `GET /api/health/live` - Liveness probe (для Kubernetes)

### Authentication

- `POST /api/auth/login` - Вход в систему

### Users

- `GET /api/users` - Список пользователей
- `POST /api/users` - Создать пользователя
- `PUT /api/users/:id` - Обновить пользователя
- `DELETE /api/users/:id` - Удалить пользователя

### Students

- `GET /api/students` - Список студентов
- `GET /api/students/group/:groupId` - Студенты группы
- `POST /api/students` - Создать студента
- `POST /api/students/bulk-import` - Массовый импорт из Excel
- `GET /api/students/download-template/:directionId` - Скачать шаблон Excel

### Tests

- `GET /api/tests` - Список тестов
- `GET /api/tests/:id` - Детали теста
- `POST /api/tests` - Создать тест
- `POST /api/tests/import` - Импорт из файла (Word/изображение)
- `POST /api/tests/:id/generate-variants` - Генерация вариантов для студентов

### Block Tests

- `GET /api/block-tests` - Список блок-тестов
- `GET /api/block-tests/:id` - Детали блок-теста
- `POST /api/block-tests` - Создать блок-тест
- `POST /api/block-tests/:id/generate-variants` - Генерация вариантов

### OMR Scanning

- `POST /api/omr/upload` - Загрузить изображение
- `POST /api/omr/check-answers` - Сканировать и проверить ответы
- `POST /api/omr/save-result` - Сохранить результат
- `GET /api/omr/results/:testId` - Получить результаты теста

### Statistics

- `GET /api/statistics` - Общая статистика системы

## 🔒 Безопасность

### Rate Limiting

API защищен от злоупотреблений с помощью rate limiting:

- **Общие API запросы**: 100 запросов / 15 минут
- **Аутентификация**: 5 попыток / 15 минут
- **Загрузка файлов**: 20 загрузок / час
- **OMR сканирование**: 50 сканирований / час
- **AI парсинг**: 30 запросов / час

### Аутентификация

Все защищенные endpoints требуют JWT токен в заголовке:

```
Authorization: Bearer <token>
```

### Роли и права доступа

- **SUPER_ADMIN**: Полный доступ ко всей системе
- **FIL_ADMIN**: Управление своим филиалом
- **TEACHER**: Управление своими группами и тестами
- **METHODIST**: Методическая работа
- **OBSERVER**: Только просмотр статистики

## 🐍 Python Скрипты

### OMR Scanner (omr_color.py)

Сканирует цветные ответные листы:
- Зеленые кружки = пустые
- Красные/темные кружки = заполненные
- Автоматическое определение колонок и вопросов
- Обработка множественных ответов

```bash
python server/python/omr_color.py <image_path>
```

### QR Scanner (qr_scanner.py)

Сканирует QR-коды на ответных листах:
- Множественные методы детекции
- Обработка разных условий освещения
- Поддержка различных масштабов

```bash
python server/python/qr_scanner.py <image_path>
```

## 📊 Мониторинг

### Логирование

Система использует структурированное логирование с цветовой кодировкой:

- 🔴 **ERROR**: Критические ошибки
- 🟡 **WARN**: Предупреждения
- 🔵 **INFO**: Информационные сообщения
- ⚫ **DEBUG**: Отладочная информация (только в development)

### Health Checks

Используйте `/api/health/detailed` для мониторинга:

```json
{
  "status": "healthy",
  "services": {
    "mongodb": { "status": "connected", "healthy": true },
    "redis": { "status": "connected", "healthy": true },
    "queue": { "status": "operational", "healthy": true }
  },
  "system": {
    "uptime": "2h 15m 30s",
    "nodeVersion": "v18.17.0"
  },
  "resources": {
    "memory": { "heapUsed": 145, "heapTotal": 256 },
    "cpu": { "user": 1234, "system": 567 }
  }
}
```

## 🔧 Скрипты

```bash
# Development
npm run dev              # Запуск с hot-reload

# Production
npm run build            # Сборка TypeScript
npm start                # Запуск production сервера

# Database
npm run create-indexes   # Создать индексы
npm run init-roles       # Инициализировать роли
npm run create-admin     # Создать администратора
npm run check-db         # Проверить базу данных
npm run clean-db         # Очистить базу данных

# Students
npm run create-7th-grade # Создать студентов 7 класса
npm run promote-students # Повысить класс студентов

# Maintenance
npm run migrate-teachers # Миграция учителей в Users
npm run reassign-teachers # Переназначить учителей
```

## 🚨 Troubleshooting

### MongoDB не подключается

```bash
# Проверьте строку подключения в .env
# Убедитесь что MongoDB запущен
mongosh "your_connection_string"
```

### Python скрипты не работают

```bash
# Проверьте установку зависимостей
pip list | grep opencv-python
pip list | grep numpy

# Переустановите если нужно
pip install --upgrade opencv-python numpy
```

### Redis ошибки

```bash
# Redis опционален, можно отключить
REDIS_ENABLED=false

# Или проверьте что Redis запущен
redis-cli ping
```

### Rate limit ошибки

Если получаете 429 ошибки, подождите указанное время или увеличьте лимиты в `server/src/middleware/rateLimiter.ts`.

## 📝 Лицензия

Proprietary - Все права защищены

## 👥 Поддержка

Для вопросов и поддержки обращайтесь к команде разработки.
