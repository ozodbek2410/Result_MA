# Исправление путей к Python скриптам и uploads

## 📋 Обзор

Это исправление решает проблему с неправильными путями к Python скриптам и директории uploads в Node.js/TypeScript приложении.

### Проблема
```
❌ Приложение искало: /var/www/resultMA/python/omr_color.py
✅ Файл находится:    /var/www/resultMA/server/python/omr_color.py
```

### Причина
PM2 запускается с `cwd: /var/www/resultMA`, а код использовал `process.cwd()` для формирования путей.

### Решение
Заменили `process.cwd()` на `__dirname` с правильным подъемом на уровни вверх до директории `/var/www/resultMA/server/`.

## 📁 Документация

| Файл | Описание |
|------|----------|
| **QUICK_SUMMARY_RU.md** | 🚀 Начните отсюда! Краткая сводка на 1 страницу |
| **PATH_FIX_SUMMARY.md** | 📖 Подробное описание всех изменений |
| **ИНСТРУКЦИЯ_ПО_ДЕПЛОЮ.md** | 🔧 Пошаговая инструкция по деплою |
| **CHECKLIST.md** | ✅ Чеклист для проверки после деплоя |
| **PATH_STRUCTURE.md** | 🗺️ Визуальная схема структуры путей |
| **deploy-path-fix.sh** | 🤖 Автоматический скрипт деплоя |

## 🚀 Быстрый старт

### Вариант 1: Автоматический деплой

```bash
bash deploy-path-fix.sh
```

### Вариант 2: Ручной деплой

```bash
cd /var/www/resultMA/server
npm run build
cd ..
pm2 restart all
pm2 logs
```

## 📝 Измененные файлы

Всего изменено **6 файлов**:

1. ✅ `server/src/routes/omr.routes.ts` - основной файл OMR логики
2. ✅ `server/src/index.ts` - статические файлы uploads
3. ✅ `server/src/services/omrQueueHandler.ts` - очередь OMR задач
4. ✅ `server/src/routes/upload.routes.ts` - загрузка файлов
5. ✅ `server/src/routes/test.routes.ts` - импорт тестов
6. ✅ `server/src/scripts/generateExcelTemplate.ts` - генерация Excel

## 🔍 Что изменилось

### До:
```typescript
const pythonScript = path.join(process.cwd(), 'server', 'python', 'omr_color.py');
const uploadDir = path.join(process.cwd(), 'uploads', 'omr');
```

### После:
```typescript
const SERVER_ROOT = path.join(__dirname, '..', '..');
const pythonScript = path.join(SERVER_ROOT, 'python', 'omr_color.py');
const uploadDir = path.join(SERVER_ROOT, 'uploads', 'omr');
```

## ✅ Проверка

После деплоя в логах PM2 должны быть:

```
✅ Upload directory ready: /var/www/resultMA/server/uploads/omr
✅ Python script exists
🐍 Python command: python3 "/var/www/resultMA/server/python/omr_color.py" ...
```

## 🧪 Тестирование

1. Загрузить OMR изображение
2. Проверить распознавание QR-кода
3. Проверить определение ответов
4. Сохранить результат

Всё должно работать без ошибок `ENOENT` или `File not found`.

## 🆘 Помощь

### Ошибка компиляции
```bash
cd /var/www/resultMA/server
npm install
npm run build
```

### Python скрипты не найдены
```bash
ls -la /var/www/resultMA/server/python/
```

### Директория uploads не создается
```bash
mkdir -p /var/www/resultMA/server/uploads/omr
chmod 755 /var/www/resultMA/server/uploads/omr
```

### PM2 не перезапускается
```bash
pm2 list
pm2 restart all
pm2 logs --lines 50
```

## 📞 Контакты

Если возникли проблемы:
1. Проверьте `CHECKLIST.md` - там есть решения типичных проблем
2. Посмотрите логи: `pm2 logs`
3. Проверьте что все файлы существуют: `ls -la /var/www/resultMA/server/python/`

## 📚 Дополнительно

- Все изменения обратно совместимы
- Не требуется изменение конфигурации PM2
- Не требуется изменение переменных окружения
- Работает на Windows, Linux, macOS

---

**Версия:** 1.0.0  
**Дата:** 2026-02-06  
**Статус:** ✅ Готово к деплою
