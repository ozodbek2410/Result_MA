# Исправление проблемы с OMR сканированием блок-тестов

## Проблема
После сканирования QR-кода в блок-тестах показывались все вопросы блок-теста (например, 30), хотя у конкретного ученика в его варианте должно быть только 25 вопросов по 2 предметам.

## Причина
При создании вариантов для блок-тестов не сохранялся `subjectId` для каждого вопроса в `shuffledQuestions`. Из-за этого при сканировании OMR система не могла определить, какие вопросы относятся к выбранным студентом предметам, и показывала все вопросы.

## Что было исправлено

### 1. Создание вариантов (`server/src/routes/blockTest.routes.ts`)
Теперь при создании вариантов для блок-тестов каждый вопрос в `shuffledQuestions` сохраняется с `subjectId`:

```typescript
for (const question of subjectQuestions) {
  const shuffled = shuffleVariants(question);
  // ВАЖНО: Сохраняем subjectId для каждого вопроса
  shuffled.subjectId = subjectTest.subjectId;
  shuffledQuestions.push(shuffled);
}
```

### 2. Обработка OMR (`server/src/routes/omr.routes.ts`)
Улучшена логика фильтрации вопросов:
- Проверяется наличие `subjectId` у вопросов
- Вопросы фильтруются по конфигурации студента (выбранные предметы)
- Учитывается количество вопросов для каждого предмета
- Правильно вычисляется `totalQuestions` на основе отфильтрованных вопросов

### 3. Скрипт обновления существующих вариантов
Создан скрипт `server/src/scripts/updateBlockTestVariants.ts` для обновления старых вариантов.

## Как применить исправления

### Для новых вариантов
Новые варианты будут создаваться автоматически с правильной структурой. Просто:
1. Перезапустите сервер
2. Создайте новые варианты для блок-тестов

### Для существующих вариантов
Есть 2 варианта:

#### Вариант 1: Пересоздать варианты (рекомендуется)
1. Удалите старые варианты через интерфейс
2. Создайте новые варианты
3. Новые варианты будут иметь правильную структуру

#### Вариант 2: Обновить существующие варианты скриптом
```bash
cd server
npx ts-node src/scripts/updateBlockTestVariants.ts
```

Скрипт:
- Найдет все варианты блок-тестов без `subjectId`
- Сопоставит вопросы с блок-тестом по тексту
- Добавит `subjectId` к каждому вопросу
- Покажет статистику обновления

## Проверка работы

После применения исправлений:

1. **Создайте новый вариант** для блок-теста
2. **Проверьте в базе данных**, что `shuffledQuestions` содержат `subjectId`:
   ```javascript
   db.studentvariants.findOne({ testType: 'BlockTest' })
   // Проверьте что shuffledQuestions[0].subjectId существует
   ```

3. **Отсканируйте OMR лист**:
   - QR-код должен правильно определиться
   - Количество вопросов должно соответствовать конфигурации студента (например, 25 вместо 30)
   - Должны показываться только вопросы по выбранным предметам

4. **Проверьте логи сервера**:
   ```
   ✅ Вопросы содержат subjectId, фильтруем по конфигурации
   📋 Selected subjects: [...]
   📊 Subject question counts: [...]
   ✅ Jami 25 ta to'g'ri javob (filtered shuffled)
   📊 Questions per subject: [...]
   ```

## Технические детали

### Структура shuffledQuestions (новый формат)
```typescript
{
  text: "Вопрос...",
  variants: [...],
  correctAnswer: "A",
  subjectId: ObjectId("..."),  // <- ДОБАВЛЕНО
  // ... другие поля
}
```

### Логика фильтрации
1. Получаем конфигурацию студента (`StudentTestConfig`)
2. Определяем выбранные предметы и количество вопросов для каждого
3. Проходим по `shuffledQuestions` и берем только вопросы:
   - С `subjectId` из выбранных предметов
   - Не превышая лимит вопросов для каждого предмета
4. Формируем `correctAnswers` только для отфильтрованных вопросов

### Обратная совместимость
Код поддерживает старый формат (без `subjectId`):
- Если `subjectId` отсутствует, используются все вопросы
- Выводится предупреждение в логи
- Рекомендуется пересоздать варианты

## Дополнительные улучшения

В коде также добавлены:
- Подробные логи для отладки
- Проверки на пустые `correctAnswers`
- Правильное вычисление `totalQuestions` из отфильтрованных вопросов
- Информативные сообщения об ошибках

## Поддержка

Если после применения исправлений проблема сохраняется:
1. Проверьте логи сервера на наличие предупреждений
2. Убедитесь, что варианты пересозданы или обновлены скриптом
3. Проверьте, что конфигурация студента (`StudentTestConfig`) существует и корректна
